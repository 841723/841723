To compile and generate the PDF version of the CV, you can use the following command in the terminal:

```bash
pdflatex diegoraulroldanuruena-cv.tex
```

To develop the CV, you can edit the `diegoraulroldanuruena-cv.tex` file using any text editor or LaTeX editor of your choice. Make sure to save the changes and recompile the document to see the updates in the generated PDF. Use:

```bash
latexmk -pdf -pvc diegoraulroldanuruena-cv.tex
```

This command will automatically recompile the document whenever you save changes to the `.tex` file, allowing you to see the updates in real-time.